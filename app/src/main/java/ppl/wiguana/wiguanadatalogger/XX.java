package ppl.wiguana.wiguanadatalogger;

public class XX {

/*
	public void recvLine(String line){
		if(started){
			int sn,diff;
			String[] strdata = line.split(",");

			if(strdata.length>4){
				sn = Integer.parseInt(strdata[3]);
				if(lastSent ==-1){
					lastSent =sn;
				}else {

					diff = sn - lastSent;
					if(diff>1){
						lostPackages = lostPackages +diff-1;
					}
					lastSent =sn;
				}
			}
			receivedPackages++;

			long mils = System.currentTimeMillis();
			String csvline=mils+","+line+","+ gpsLat +","+ gpsLon +","+ gpsAlt +","+ gpsAccuracy;
			data.add(csvline);
			//display.append(""+receivedPackages+" --- "+csvline+"\n");
			receivedPackagesTv.setText(""+ receivedPackages);
			lostPackagesTv.setText(""+ lostPackages);
			//lostPackagesTv.setText(""+receivedPackages);

		}

	}
*/

}
